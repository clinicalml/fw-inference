scripts to setup model folders

Rahul G. Krishnan
rahul@cs.nyu.edu
----------------
Contains python scripts for different tasks
driver-Setup.py : Main driver program
Instructions
	-Set the variables convExec (points to uai2fg, a converter packaged within libDAI) and exactExec (exact is a wrapper in libdai-tools)
	-Set the string in folderName to point to the folder you would like to setup (eg:SyntheticGrids_5x5)
	-Fill stepsToDo (Always do step 2 at the minimum and do not run step 1/3 for large graphs)
	-run the file

convertUAItoMAT.py : Convert from UAI to MATLAB format

computeExact.py : Compute exact values using junction tree (libDAI)
exactToMAT.py : Move results from JT to MATLAB format

perturbMAP.py :Small implementation of perturb and MAP (requires toulbar2)  
runPerturbAndMAP.py
